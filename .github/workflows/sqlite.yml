on:
  pull_request:
    paths: &paths
      - 'resources/**'
      - 'src/**'
      - 'tests/**'
      - '.github/workflows/sqlite.yml'
      - 'composer.json'
      - 'phpunit.xml.dist'

  push:
    branches: ['master']
    paths: *paths

name: sqlite

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  tests:
    uses: yiisoft/actions/.github/workflows/db-sqlite.yml@auto-detect-versions
